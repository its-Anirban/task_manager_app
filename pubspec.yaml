name: task_manager_app
description: A Flutter app for managing tasks using Spring Boot backend.

environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2